apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: humor-ingress
  namespace: humor-game
spec:
  rules:
  - host: gameapp.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
